#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int w,a,c;
    scanf("%d%d%d",&w,&a,&c);
     int t=a*75+c*30;
    if(t<=w)
        printf(" boat is stable");
       else
           printf("boat is drown");
return 0;
}
